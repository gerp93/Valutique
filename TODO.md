# TODO

This app's own backlog of future features and fixes — not a KVG_Standards
compliance checklist (see [KVG_Standards](https://github.com/gerp93/KVG_Standards)
and this repo's `REPO_SCOPE.md` entry for that). Just what's not built yet.

## Features

AI Suggested Features...don't implement until told. 
- Duplicate detection — flag when newly added photos look like an item already in the collection
- Export: CSV / PDF inventory report for insurance, and a printable collection valuation summary
- Bulk edit — apply a field value or location to a multi-select of items
- Saved searches and filters (by field value, condition, value range, appraisal age)
- Value-over-time chart per item and per collection, from the appraisal history already stored
- eBay Marketplace Insights integration for real sold-price comps, if API access is ever granted
- Sets and groupings (a boxed multi-piece set that is worth more together than apart)
- Photo editing on ingest: crop, rotate, auto-straighten before the AI call
- Import from an existing spreadsheet into a collection's custom fields

## Fixes

-
